{
  "name": "@claude-viz/hooks",
  "version": "0.1.0",
  "description": "Hook scripts for Claude CLI integration",
  "bin": {
    "claude-viz-hook": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@claude-viz/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  }
}
